# Upgrade deployment and rollback — local artifacts only

Release set: Parent **0.33.0**, Child **0.12.0**, Tools **0.26.0**, Registry **1.4.0**, Content Contract **2.1.0**, Publish Package **1.0.0**. No production installation, database update, cache purge or provider configuration was performed by this task.

1. Back up the database, current three component directories, site options, server settings and active destination catalog revision. Preserve the existing approved Trip.Planner option/filter destination. Record the live versions before replacing anything.
2. Verify SHA-256 against `dist/release-manifest.txt`. Install the parent ZIP first; install/activate the child ZIP second; install/activate Tools third. Test in staging before production. The child still depends on the parent; plugin deactivation now provides readable shortcode fallbacks.
3. This upgrade does not overwrite administrator-edited article/page bodies. Existing guide/entity metadata and component IDs remain valid. The nine-record built-in catalog is a bundled JSON fallback. No catalog import is required to keep current coverage. For a reviewed expanded catalog, validate against `contracts/destination-catalog.schema.json`, then use the administrator-only catalog endpoint and retain the returned revision.
4. Regenerate WordPress Media sizes for existing editorial attachments if they lack responsive metadata. Bundled theme derivatives are already in the ZIP. Purge page/CDN caches and versioned CSS/JS after the complete set is installed, and after catalog or entity-publication changes when the CDN caches rendered links. A site purge is operational work; no cache plugin/server configuration was changed here.
5. Ordinary anonymous HTML may use normal full-page caching. Bypass all recognition, resolver and catalog POST responses and respect `private, no-store`, including errors. Never cache POST request bodies. Bypass authenticated/preview/password-protected content. A public Taxi URL with `entity_key` must vary by the complete query string or bypass HTML caching; never collapse different destinations into one cached card.
6. Configure only the chosen HTTPS gateway's server secrets: `STC_PLACE_VISION_ENDPOINT`, `STC_PLACE_VISION_TOKEN`, and optional resolver equivalents. Confirm gateway response schema, vendor retention and deletion policy before enabling uploads. Set provider-level spend caps as well as `STC_VISION_DAILY_LIMIT` / `STC_VISION_CONCURRENCY`. The default plugin ceiling is 100 vision attempts/day and two concurrent upstream calls. The status shown before upload checks configuration, not live vendor uptime.
7. Review host limits: upload file limit at least 20M, post/reverse-proxy body limit at least 64M, and request timeouts sufficient for the maximum 35-second upstream call plus image processing. A 512M PHP memory budget is a starting point for testing large pixel inputs, not a host capacity guarantee; load-test the configured number of workers. Ensure WordPress's image editor can decode JPEG/PNG/WebP. Keep PHP temporary-directory cleanup enabled for interrupted workers. Configure trusted proxy real-IP normalization on the server; the plugin ignores spoofable forwarding headers.
8. Disable body/secret logging in gateway, reverse proxy and APM. Do not record multipart content, base64, user hints/queries or Authorization. Plugin temporary-file behavior does not establish the external provider's retention policy. Update the live Privacy Policy to the actual provider terms, without claiming unverified immediate deletion.
9. Optional ticket rules require reviewed date, source, validity end, lead days and an approved entity-specific ticket URL. Current bundled historic rules remain unknown until reviewed. Preserve the approved Trip.Planner shortlink `https://www.trip.com/t/bCPFQ85ZHW2`; confirm its real redirect and affiliate attribution in the production account. No affiliate parameters were invented.
10. Refresh the CMS's generated contracts from WordPress, then publish/read back a **draft** containing the new optional variants. The separate CMS repository has not been integrated by this task. Recheck home/entity links, 840/841px More, Share copy fallback, server TOC, 1–4 image submit/cancel, partial Taxi/Driver Mode, unknown tickets and plugin-unavailable behavior.

Rollback: restore the prior three directories as a set, restore relevant site options/DB backup if a migration or CMS publish occurred, restore the previous catalog revision with the rollback endpoint, and purge HTML/assets. Catalog rollback swaps the active and previous pointers; take your own export before multiple successive imports. Do not roll back article content just because a prior screenshot differs. The initial dirty workspace was preserved in the local baseline patch and historical handoff archive.

Only the user/server administrator can perform the production steps above: this workspace has no production deployment authorization, hosting login, selected gateway credentials, or external CMS checkout.
