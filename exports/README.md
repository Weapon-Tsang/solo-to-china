# WordPress Content Exports

The original WordPress export file was reviewed locally:

- `solotochina.WordPress.2026-08-21.xml`

It contains early-stage WordPress content plus author metadata. A sanitized copy was prepared in the local audit workspace, but XML exports are not committed by default.

When a content export is needed for migration, generate a fresh WordPress export from the live site and review it for user/account metadata before committing.
